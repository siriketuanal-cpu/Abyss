{
  "name": "abyss-timer",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "lint": "vite build --emptyOutDir",
    "preview": "vite preview --port 3000"
  },
  "devDependencies": {
    "vite": "^6.1.0"
  }
}
